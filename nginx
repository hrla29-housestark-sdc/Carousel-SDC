worker_processes 1;

events {
  worker_connections 1024;
}

http {
  upstream myprojects{
    server ec2.something.com;
    server ec2.somethingelse.com;
  }
  server {
    listen 80;
    location / {
        proxy_pass http://myprojects;
    }
  }
}